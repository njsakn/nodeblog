<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %> 
<body>
    <%- include('./partials/nav.ejs') %> 
    <% const name ='mario' %> 
    <p><%= name //stampo il nome %>  </p>
    <div class="blogs content">
        <h2>All Blogs</h2>
        
      <% if (blogs.length>0) { %>

           <% blogs.forEach(blog => { %>
             
            <h3 class="title"> <%= blog.title %> </h3>
            <p class="testo">  <%= blog.testo %>   </p>
            <p></p>

      <% }) %> 
      <% }else  {%> 
            <h1> Non ci sono blog al moemento</h1>
        <% }%> 
    </div>
</body>
</html>
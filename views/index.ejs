<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %> 
<body>
    <%- include('./partials/nav.ejs') %> 
    <% const name ='mario' %> 
    <div class="blogs content">
        <h1 >Chat globale</h2>
        
      <% if (blogs.length>0) { %>

           <% blogs.forEach(blog => { %>
             
            <h2 class="title"> <%= blog.title %> </h3>
            <h3 class="testo">  <%= blog.testo %>   </p>
            <p></p>

      <% }) %> 
      <% }else  {%> 
            <h1> Non ci sono blog al moemento</h1>
        <% }%> 
    </div>
</body>
 <script>
    
    function timedRefresh(timeoutPeriod) {
	setTimeout("location.reload(true);",timeoutPeriod);
}

   window.onload = timedRefresh(2000);


    </script>
</html>
